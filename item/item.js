class item{

  constructor item(x, y, sprite, id){

    this.x = x;
    this.y = y;
    this.sprite = "";
    this.id = id;

  }

  update(){

    if (player.x && player.y === x, y) {

    player.playerInventory.push(id)

    return true;
    //test
    }
  }
  
  draw(){

  }

}
